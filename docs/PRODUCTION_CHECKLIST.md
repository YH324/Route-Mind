# RouteMind 生产环境上线检查清单

## 基础设施

- [ ] 服务器资源配置（CPU / 内存 / 磁盘）
- [ ] 反向代理配置（Nginx / Caddy）
- [ ] HTTPS 证书配置
- [ ] 域名解析配置
- [ ] CDN 配置（静态资源加速）
- [ ] 防火墙规则配置

## 数据层

- [ ] 高德 API Key 申请并配置
- [ ] 美团 API Key 申请并配置（如需要）
- [ ] Redis 缓存服务部署
- [ ] 数据备份策略制定
- [ ] POI 数据全量导入
- [ ] 路网数据全量导入
- [ ] 评分数据全量导入

## 应用层

- [ ] `HttpApiClient` 实现完成
- [ ] 本地 POI 适配器 → `HttpApiClient` 切换验证
- [ ] API 限流配置（防止触发平台配额）
- [ ] 请求超时配置合理
- [ ] 错误降级策略生效
- [ ] 日志收集配置（文件 / 云日志）
- [ ] 监控告警配置

## 模型与推荐质量

- [ ] `result.model.candidate_pipeline` 在 v1/v2 响应中稳定返回
- [ ] 每个 `route` / `recommendations` item 都包含 `recommendation_basis`
- [ ] 天府广场默认中心坐标校验为 `104.06476,30.65705`
- [ ] 服务范围拦截生效：广州/重庆/青羊区等未覆盖请求返回 `UNSUPPORTED_SERVICE_AREA`，响应包含 `service_area`
- [ ] 低价值 POI 过滤覆盖停业/装修、入口/门岗、停车/快递/充电、民宿房源、普通销售经营点
- [ ] 单点“逛街/购物”优先真实商场本体，非购物路线不使用普通购物或 `其他` 类型凑点
- [ ] 路网起点不可用时的 `start_fallback` 降级可观测
- [ ] 严格时间/路网约束下无法构成主路线时返回带时间轴和 `polyline` 的“顺序候选路线”，不硬凑低质量路线
- [ ] `feature_ranker_v1.5` 数据驱动排序信号可观测：`review_count_estimate`、`popularity_adjustment`、`brand_popularity_bonus`、`brand_signal`、`entity_quality_adjustment`、`entity_quality_signals`
- [ ] `time_budget_source` 可观测：显式时长不被覆盖，未说明时长按单点/组合/半日/一日/商务/夜间场景推断
- [ ] `review_summary` 可展示：卡片和地图弹窗使用用户可读口碑摘要，不暴露内部评分字段或模型术语
- [ ] 模糊短句返回 `clarification_options`，同一 `session_id` 下 follow-up 能沿用上次位置和类型
- [ ] 春熙/太古里火锅回归：优先完整正餐火锅门店，过滤麻辣烫/冒菜/甜品误分类与共享充电等附属设施，同品牌不连续占满榜单
- [ ] 非火锅单点回归：茶馆过滤茶饮/棋牌，咖啡优先 coffee/cafe 名称，公园过滤绿地超市/商业广场/普通景点，中餐过滤酒店/会展，酒吧过滤健身/营养 club，超市+小吃路线同时覆盖两个明确类型
- [ ] 组合意图回归：太古里逛街+咖啡返回商场+饮品，九眼桥喝酒+夜宵返回酒吧+小吃，出差午餐+咖啡返回中餐+饮品
- [ ] `result.model.candidate_pipeline.entity_type_corrections` 可观测，类型纠偏异常增长时能定位数据索引质量问题
- [ ] LLM 候选评审只允许返回已存在的 `poi_id`，网络/API 异常时 `llm_candidate_review.used=false` 且本地排序继续生效
- [ ] 模型回归测试通过：`python -m unittest tests.test_backend_model_contract`

## 前端

- [ ] 生产环境构建优化
- [ ] 静态资源压缩
- [ ] 首次进入页面展示成都武侯区/锦江区服务范围公告，用户确认后不重复打断
- [ ] 预选场景覆盖春熙半日、锦里武侯、九眼桥夜游、太古里咖啡、出差午餐
- [ ] 前端错误监控接入（Sentry 等）
- [ ] 移动端适配测试
- [ ] 右侧结果列表可滚动，5 个以上推荐不被底部操作区或移动端导航遮挡
- [ ] 地图 marker 和结果卡片有可发现的点击详情入口，路线变体能画出 polyline
- [ ] 各浏览器兼容性测试

## 安全

- [ ] API Key 存储安全（环境变量 / 密钥管理）
- [ ] CORS 配置合理
- [ ] 请求体大小限制
- [ ] SQL 注入防护（如有数据库）
- [ ] XSS 防护

## 监控与运维

- [ ] 健康检查端点 (`/api/health`) 接入监控
- [ ] 就绪检查端点 (`/api/ready`) 接入负载均衡
- [ ] 指标端点 (`/api/metrics`) 接入 Prometheus / Grafana
- [ ] 告警规则配置（延迟 / 错误率 / 可用性）
- [ ] 日志轮转配置
- [ ] 故障恢复手册

## 压测

- [ ] 单接口压测（目标：QPS > 10）
- [ ] 全链路压测
- [ ] 并发规划请求保持响应契约一致（v1/v2、模型字段、推荐依据）
- [ ] 缓存命中率验证（目标：> 80%）
- [ ] 内存泄漏检查

## 法律合规

- [ ] 高德地图使用条款确认
- [ ] 用户隐私政策制定
- [ ] 数据安全合规检查
- [ ] 开源协议合规检查
